#include <iostream>
 
using namespace std;
 
int main() {
    
    int c1, n1, c2, n2;
    double p1, p2, V;
    
scanf("%d %d %lf\n", &c1, &n1, &p1);

scanf("%d %d %lf", &c2, &n2, &p2);

V = n1*p1 + n2*p2;

printf("VALOR A PAGAR: R$ %.2lf\n", V);
    
    return 0;
}
